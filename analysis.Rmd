---
title: "Untitled"
author: "Jack Leary"
date: "`r Sys.Date()`"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, message = FALSE, warning = FALSE, comment = NA)
set.seed(312)  # lucky seed
```

# Libaries 

```{r}
library(targets)
library(tidyverse)
```

# Data 

```{r}
sim_data_files <- list.files("/blue/rbacher/j.leary/repos/scLANE-Sims/_targets/objects/", pattern = "*sim*")
scLANE_res_files <- list.files("/blue/rbacher/j.leary/repos/scLANE-Sims/_targets/objects/", pattern = "scLANE*")
test_results <- tar_read("scLANE_res_DEG_05_CELLS_100")
test_results$testDynamic_results_tidy[[1]]
dataset <- tar_read("lung_sim_DEG_05_CELLS_100")
SingleCellExperiment::rowData(dataset) %>% 
  as.data.frame() %>% 
  mutate(gene = rownames(.))
purrr::map2(data_list, result_list, function(x, y) {
  
})
```

# Analysis 